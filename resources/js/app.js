import './bootstrap';
import './admin';

const myApp = Vue.createApp({
    data() {
        return {

        };
    },
    mounted() {
        window.addEventListener('load', () => {
        });
    },
    watch: {
    },
    components: {
    },
    computed: {
    },
    methods: {
    },
});

myApp.mount('#app');